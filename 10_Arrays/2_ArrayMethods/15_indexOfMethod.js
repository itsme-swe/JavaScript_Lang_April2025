/*
💥 The indexOf() method is used to find the index of a particular element in an array, and if the element is not present method returns -1.
*/

let a = ["Hello", "GFG", "JS"];

console.log(a.indexOf("GFG"));
