let a = [20, 5, 10, 50, 60, 70];

console.log(a.length);  // 6

/*
🔸 The length property of an array returns the number of elements in the array. It automatically updates as elements are added or removed.
*/
