let a = ["Harsh", 69.5, 32, "SWE"];

console.log(
  "Using Array.isArray method to recognize array: ",
  Array.isArray(a)
);

// Using Array.isArray method to recognize array:  true

console.log("Using instanceof method:", a instanceof Array);

// Using instanceof method: true

/*
These are two methods by which we can recognize a JavaScript array:
💥Array.isArray()
💥instanceof method
*/
