let str = "Geeks for Geeks";

let subPart = str.slice(4, 9);

console.log(subPart); // s for

console.log();

let str1 = "Ram is going to school";

let sub = str1.slice(-6);

console.log(sub); //  school

/*
🔸 The slice() method in JavaScript is used to extract a portion of a string or an array without modifying the original value. And, the slice() method in JavaScript does not reverse the substring—it only extracts a portion of the string.
*/
